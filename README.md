# [Your Name] — Your Hand

A résumé dealt as a hand of cards. React + TypeScript on Vite, static,
deployed on Vercel.

```sh
npm install
npm run dev      # http://localhost:5173
npm run build    # typecheck + production build to dist/
```

Fill in `src/data/*.ts`; everything in `[brackets]` is placeholder. Replace
`public/resume.pdf`. Set `VITE_SITE_URL` in Vercel to your origin so the
social preview image resolves. See `CLAUDE.md` for the system and rules.
